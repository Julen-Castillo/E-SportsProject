<?xml version="1.0" encoding="utf-8"?>
<xs:schema xlmns:xs="http://www.w3.org/2001/XMLSchema">
    <!-- JORNADAS -->
    <xs:element name="jornadas" type="tipo_jornadas"/>
    <!-- JORNADA -->
    <xs:complexType name="tipo_jornadas">
        <xs:sequence>
            <xs:element name="jornada" type="tipo_jornada" minOccurs="1" maxOccurs="unbounded"/>
        </xs:sequence>
        <xs:attribute name="id" type="xs:integer"/>
    </xs:complexType>
    <!-- PARTIDOS -->
    <xs:complexType name="tipo_jornada">
        <xs:sequence>
            <xs:element name="partidos" type="tipo_partidos"/>
        </xs:sequence>
        <xs:attribute name="fecha_inicio" type="xs:date"/>
        <xs:attribute name="fecha_fin" type="xs:date"/>
    </xs:complexType>
    <!-- PARTIDO -->
    <xs:complexType name="tipo_partidos">
        <xs:sequence>
            <xs:element name="partido" type="tipo_partido" minOccurs="1" maxOccurs="unbounded"/>
        </xs:sequence>
        <xs:attribute name="id" type="xs:integer"/>
    </xs:complexType>
    <!-- INSIDE PARTIDO -->
    <xs:complexType name="tipo_partido">
        <xs:sequence>
            <xs:element name="local" type="xs:string"/>
            <xs:element name="visitante" type="xs:string"/>
            <xs:element name="encuentro" type="tipo_encuentro"/>
            <xs:element name="fecha" type="xs:date"/>
            <xs:element name="hora" type="xs:time"/>
            <xs:element name="ciudad" type="xs:string"/>
            <xs:element name="estadio" type="xs:string"/>
            <xs:element name="ganador" type="xs:string"/>
            <xs:element name="resultado" type="xs:string"/>
            <xs:element name="mvp" type="xs:string"/>
        </xs:sequence>
    </xs:complexType>

    <!-- DEFINIR RESTRICCION TIPO ENCUENTRO -->
    <xs:simpleType name="tipo_encuentro">
        <xs:restriction base="xs:string">
            <xs:enumeration value="Amistoso"/>
            <xs:enumeration value="Liga"/>
        </xs:restriction>
    </xs:simpleType>    
</xs:schema>
